import 'package:flutter/material.dart';

class VideoMarche extends StatelessWidget {
  const VideoMarche({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text("Liste des vidéos"),
    );
  }
}